# Copy to .env (gitignored) and fill in your own key to run real LLM stages.
# Without this set, jscc/llm_client.py falls back to StubExtractionClient —
# the pipeline runs end-to-end but returns a placeholder, not a real
# extraction. See jscc/llm_client.py for the fallback logic.
ANTHROPIC_API_KEY=
