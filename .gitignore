# Python
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
.pytest_cache/
.ruff_cache/
.venv/
venv/


# JSCC data safety (D7 M1/M2 — locked in Slice A4.5a)
data/*.db
data/*.db-shm
data/*.db-wal
data/*.db-journal
!data/synthetic.db
*.private.*
profile.private.yaml
contacts.private.*
.env
.env.*
!.env.example

# B3b smoke test snapshot -- regenerable, and its live job-board URLs embed
# numeric IDs that trip the phone-pattern scanner as a digit-count false
# positive (same class as A9/B2's; see CHANGELOG). Not gitignored dual-use
# data, just noisy to keep tracked -- results are summarized in jscc.md/CHANGELOG.
docs/smoke-test-results.md

# IDE / OS
.vscode/
.idea/
.DS_Store
Thumbs.db
